projeto-crud-credencials/
│
├── 📚 DOCUMENTAÇÃO
│   ├── README.md                    (Como usar e integrar)
│   ├── DYNAMODB_SCHEMA.md           (Schema DynamoDB)
│   └── STRUCTURE.txt                (Este arquivo)
│
├── 🧪 TESTES & SCRIPTS
│   └── test.sh                      (Script para rodar testes)
│
├── 📦 DEPENDÊNCIAS
│   └── go.mod                       (AWS SDK v2 + Gin)
│
├── 🎯 CAMADA: Handler (HTTP)
│   └── pkg/handler/varconfig/
│       ├── doc.go                   (Package comment)
│       ├── handler.go                (Manipuladores HTTP)
│       │   ├── ListByBenchmark()     (GET /.../{benchmark_id})
│       │   ├── Create()             (POST /.../{benchmark_id})
│       │   ├── GetByID()            (GET /.../{id})
│       │   ├── Update()             (PUT/PATCH /.../{id})
│       │   └── Delete()             (DELETE /.../{id})
│       ├── types.go                 (Interface VarConfigUseCase)
│       ├── validators.go            (Validação de entrada)
│       └── init.go                  (Wiring de dependências)
│           ├── InitHandler()
│           └── RegisterRoutes()
│
├── 🔧 CAMADA: Service (Negócio)
│   └── internal/service/domain/varconfig/
│       ├── models.go                (Struct de domínio)
│       │   └── VarConfig{ID, OrgID, BenchmarkID, Payload, ...}
│       ├── ports.go                 (Interface de repositório)
│       │   └── VarConfigRepository (contrato)
│       ├── service.go               (Lógica de negócio)
│       │   ├── NewService()
│       │   ├── Create()
│       │   ├── GetByID()
│       │   ├── ListByBenchmark()
│       │   ├── Update()
│       │   └── Delete()
│       └── assertions.go            (Verificação de conformidade)
│           └── var _ VarConfigRepository = (*Repository)(nil)
│
└── 💾 CAMADA: Storage (DynamoDB)
    └── internal/storage/dynamodb/varconfig/
        ├── repository.go            (Implementação DynamoDB)
        │   ├── DynamoDBModel (PK+SK structure)
        │   ├── NewRepository()
        │   ├── Save() → PutItem
        │   ├── FindByID() → GetItem
        │   ├── FindAllByBenchmark() → Query
        │   ├── Update() → UpdateItem
        │   ├── Delete() → DeleteItem
        │   ├── buildPK()
        │   ├── buildSK()
        │   └── fromModel()
        └── assertions.go            (Verificação de conformidade)
            └── var _ VarConfigRepository = (*Repository)(nil)

══════════════════════════════════════════════════════════════════════

FLUXO DE UMA REQUISIÇÃO:

HTTP Request (Gin)
    ↓
pkg/handler/varconfig/handler.go
  ├─ Valida params (orgId, benchmark_id, id)
  ├─ Faz bind do JSON body
  ├─ Chama ValidateCreateRequest()
  └─ Monta VarConfig e chama service
    ↓
internal/service/domain/varconfig/service.go
  ├─ Valida regras de negócio
  ├─ Define timestamps
  └─ Chama repository
    ↓
internal/storage/dynamodb/varconfig/repository.go
  ├─ Constrói PK = "ORG#{orgId}#BENCH#{benchmark_id}"
  ├─ Constrói SK = "VARCONFIG#{id}"
  ├─ Executa operação DynamoDB otimizada
  └─ Retorna dados convertidos
    ↓
HTTP Response (JSON)

════════════════════════════════════════════════════════════════════════

ROTAS DA API:

Coleção
  GET    /org/{orgId}/compliance/variables/{benchmark_id}
  POST   /org/{orgId}/compliance/variables/{benchmark_id}

Específico
  GET    /org/{orgId}/compliance/variables/{benchmark_id}/{id}
  PUT    /org/{orgId}/compliance/variables/{benchmark_id}/{id}
  PATCH  /org/{orgId}/compliance/variables/{benchmark_id}/{id}
  DELETE /org/{orgId}/compliance/variables/{benchmark_id}/{id}

══════════════════════════════════════════════════════════════════════

OPERAÇÕES DYNAMODB:

Create (Save)
  └─ PutItem com PK + SK

Read (FindByID)
  └─ GetItem com PK + SK

List (FindAllByBenchmark)
  └─ Query com PK (sem Scan!)

Update
  └─ UpdateItem com PK + SK

Delete
  └─ DeleteItem com PK + SK

════════════════════════════════════════════════════════════════════════

ESTRUTURA DE CHAVES:

PK  = "ORG#{orgId}#BENCH#{benchmark_id}"
SK  = "VARCONFIG#{id}"

Exemplo Real:
PK  = "ORG#123#BENCH#pci-dss-v3.2.1"
SK  = "VARCONFIG#1640995200000000000"

════════════════════════════════════════════════════════════════════════